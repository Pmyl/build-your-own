[package]
name = "timer"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib"]

[dependencies]
build-your-own-utils = { path = "../../utils" }
build-your-own-macros = { path = "../../macros" }
crossterm = "0.29.0"
